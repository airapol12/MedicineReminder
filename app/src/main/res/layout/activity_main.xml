<?xml version="1.0" encoding="utf-8"?>

<!-- Need DrawerLayout for the navigation drawer or fragment below
    Need ID as well for the DrawerLayout-->
<android.support.v4.widget.DrawerLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="scratchlab.com.ph.a1materialdesignslide.MainActivity">

        <!-- To include the toolbar -->
        <include
            android:id="@+id/app_bar"
            layout="@layout/app_bar"/>


    </RelativeLayout>

    <!-- To include the navigation drawer -->
    <!-- You will notice the clickable grey area on the left side,it is becoz of the layout_gravity-->

    <fragment
        android:id="@+id/fragment_navigation_drawer"
        android:layout_width="@dimen/nav_drawer_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:layout="@layout/fragment_navigation_drawer"
        android:name="scratchlab.com.ph.medicinereminder.NavigationDrawerFragment"
        tools:layout="@layout/fragment_navigation_drawer" />

</android.support.v4.widget.DrawerLayout>